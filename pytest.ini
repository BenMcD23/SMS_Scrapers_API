[pytest]
# Tests import from app/ (core, routers, scripts, database, api), which is the single
# import root. This is what replaced the five
# different PYTHONPATH incantations the test files each used to document.
pythonpath = app
testpaths = app

# Show the reason for every non-passing outcome rather than a bare letter.
addopts = -ra
