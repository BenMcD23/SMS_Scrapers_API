apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: sms

resources:
  - ../../base
  - namespace.yaml

patches:
  - path: config.patch.yaml

# Updated by .github/workflows/release.yml on every VERSION bump to main.
images:
  - name: ghcr.io/benmcd23/sms-api
    newTag: 1.0.0
